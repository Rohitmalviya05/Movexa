{
  "name": "movexa-project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix logistics-app run dev",
    "install:all": "npm install --prefix logistics-app && npm install --prefix frontend",
    "build": "npm --prefix frontend run build",
    "render-build": "npm install --prefix logistics-app && npm install --prefix frontend && npm --prefix frontend run build",
    "start": "npm --prefix logistics-app run start",
    "render-start": "npm --prefix logistics-app run start"
  }
}
